name: Second Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: first-step
        run: |
          echo "Hello"
          echo "World"
      - name: second-job
        run: |
          echo "Done"
          echo "Goodbye!"
        

